---
const allPosts = await Astro.glob("../pages/posts/*.md");
---

<section class="blogpage ww-full md:w-3/4">
  <p>Here you can find tutorials or resources I find useful.</p>
  <ul class="mt-12 list-decimal font-inter-regular pl-12">
    {
      allPosts.map((post) => (
        <li>
          <a href={post.url}>{post.frontmatter.title}</a>
        </li>
      ))
    }
  </ul>
</section>
